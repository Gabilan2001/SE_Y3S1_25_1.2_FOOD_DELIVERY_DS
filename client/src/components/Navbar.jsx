import React from "react";

const Navbar = () => {
  return (
    <div className="navbar">
      <h2>Food Delivery Admin Panel</h2>
      <div>
        <button>Logout</button>
      </div>
    </div>
  );
};

export default Navbar;
